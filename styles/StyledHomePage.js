import styled from 'styled-components';

export const StyledHome = styled.main`
  position: relative;
  top: 0;
  height: 100vh;
  background: white;
  color: white;

  p {
    margin-bottom: 0.5rem;
  }
`;
